

<div class="objectList small">
	<div class="blockCornerLeft"></div>
	<div class="blockElementHeader sixH">
	<span id="item_count">Listing newsletters</span>
	</div>
	<div class="blockCornerRight"></div>
	<div class="contentList filtered sixH">
          <table>
  <tr>
  </tr>
<% @newsletters.each do |newsletter| %>
  <tr>
    <td><%= newsletter.title %></td>
    <td><%= link_to 'Show', newsletter %></td>
    <td><%= link_to 'Edit', edit_admin_newsletter_path(newsletter) %></td>
    <td><%= link_to 'Destroy', newsletter, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>
		</div>
	
	
</div>

